hideModal();
<% select = content_tag('select', content_tag('option') + issue_category_tree_options_for_select(@project.issue_categories, :selected => @category.id), :id => 'issue_category_id', :name => 'issue[category_id]') %>
$('#issue_category_id').replaceWith('<%= escape_javascript(select) %>');
